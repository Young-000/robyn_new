# 현재 아키텍처 및 데이터 저장 방식

## 📊 현재 상태 (브라우저 알림 POC)

### 데이터 저장 방식
**✅ LocalStorage 사용** (브라우저 로컬 저장소)

- 루틴 정보는 브라우저의 LocalStorage에 저장됩니다
- 사용자 인증 없이 동작합니다
- 단일 브라우저, 단일 사용자 환경입니다
- **DB 연결 불필요** ✅

### 필요한 것
1. **API 키만 있으면 됩니다!** ✅
   - 날씨 API 키 (OpenWeatherMap)
   - 교통 API 키 (공공데이터포털)

2. **DB 연결 불필요** ✅
   - 모든 데이터는 브라우저 LocalStorage에 저장
   - 서버 없이 동작

### 작동 방식
```
브라우저 (Frontend)
├── LocalStorage (루틴 저장)
├── Web Notifications API (알림)
└── 외부 API 호출 (날씨/교통 정보)
    ├── OpenWeatherMap API
    └── 공공데이터포털 API
```

---

## 🔄 다음 단계 (휴대폰 푸시 알림)

### 필요한 것들

#### 1. 백엔드 서버 ⚠️
- Node.js + Express 서버
- 루틴 정보 저장 및 관리
- 알림 스케줄링 (Cron Job)

#### 2. 데이터베이스 ⚠️
- PostgreSQL 또는 MongoDB
- 사용자 정보 저장
- 루틴 정보 저장
- 알림 히스토리 저장

#### 3. 사용자 인증 ⚠️
- JWT 토큰 기반 인증
- 소셜 로그인 (Google, Kakao 등)

#### 4. 푸시 알림 서버 ⚠️
- Service Worker 등록
- Push API 서버
- FCM (Firebase Cloud Messaging) 또는 Web Push Protocol

---

## 📋 단계별 요구사항

### Phase 1: 브라우저 알림 POC (현재) ✅
- [x] API 키만 필요
- [x] DB 연결 불필요
- [x] 서버 불필요
- [x] LocalStorage 사용

### Phase 2: 휴대폰 푸시 알림 (다음 단계) ⚠️
- [ ] 백엔드 서버 필요
- [ ] 데이터베이스 필요
- [ ] 사용자 인증 필요
- [ ] 푸시 알림 서버 필요

---

## 🎯 결론

### 지금은?
**API 키만 있으면 됩니다!** ✅
- 날씨 API 키
- 교통 API 키
- `.env` 파일에 설정
- 끝!

### 나중에 (푸시 알림)?
**DB와 서버가 필요합니다** ⚠️
- PostgreSQL/MongoDB 설정
- 백엔드 서버 구축
- 사용자 인증 구현
- 푸시 알림 서버 구축

---

## 💡 현재 구조의 장점

1. **간단함**: API 키만 설정하면 바로 사용 가능
2. **빠른 시작**: DB 설정, 서버 구축 불필요
3. **프로토타입 검증**: 핵심 기능만 빠르게 테스트
4. **오프라인 가능**: LocalStorage는 오프라인에서도 동작

## ⚠️ 현재 구조의 제한사항

1. **단일 브라우저**: 다른 브라우저/기기에서 접근 불가
2. **데이터 동기화 없음**: 여러 기기 간 동기화 불가
3. **브라우저 열려있어야 함**: 닫으면 알림 안 옴
4. **사용자 인증 없음**: 개인화 기능 제한

---

**지금은 API 키만 설정하시면 됩니다!** 🚀
