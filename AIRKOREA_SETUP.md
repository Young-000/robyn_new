# AirKorea 미세먼지 API 설정 가이드

## ✅ 미세먼지 정보는 AirKorea API에서 받습니다!

AirKorea (한국환경공단) API를 사용하도록 구현했습니다.

## 🚀 설정 방법

### Step 1: 공공데이터포털에서 API 신청
1. https://www.data.go.kr/ 접속
2. 검색창에 **"AirKorea 대기오염"** 또는 **"한국환경공단 에어코리아"** 검색
3. **"한국환경공단_에어코리아_대기오염정보"** 선택
4. **"활용신청"** 클릭
5. 활용 목적 작성 후 신청 완료 (보통 즉시 승인)

### Step 2: 인증키 확인
- 기상청 API와 동일한 인증키 사용 가능
- 마이페이지 → 인증키 관리에서 확인

### Step 3: .env 파일에 추가
`frontend/.env` 파일에 다음 추가:

```env
# 기상청 API (날씨 정보)
VITE_KMA_API_KEY=여기에_기상청_API_키_입력

# AirKorea API (미세먼지 정보) ⭐
VITE_AIRKOREA_API_KEY=여기에_AirKorea_API_키_입력
```

## 📋 API 조합

### 추천 조합 ⭐
1. **기상청 API** (날씨) + **AirKorea API** (미세먼지)
   - 한국어 지원
   - 정확한 한국 데이터
   - 무료

### 대체 조합
2. **OpenWeatherMap API** (날씨 + 미세먼지)
   - 하나의 API로 모두 제공
   - 영어 (한국어 제한적)

## ✅ 구현 완료

- ✅ AirKorea API 서비스 클래스 생성
- ✅ 시도명 기반 측정소 찾기
- ✅ 미세먼지 정보 조회
- ✅ 기상청 API와 통합

## 📊 제공 정보

- PM10 (미세먼지)
- PM2.5 (초미세먼지)
- 등급 (좋음/보통/나쁨/매우 나쁨/위험)
- 측정소명
- 측정 시간

## 💡 참고

- AirKorea API는 공공데이터포털 인증키 사용
- 기상청 API와 동일한 키 사용 가능
- 실시간 데이터 (1시간 단위 업데이트)

---

**AirKorea API 키를 발급받아 `.env` 파일에 추가하시면 정확한 한국 미세먼지 정보를 받을 수 있습니다!** 💨
