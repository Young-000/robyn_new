import { describe, it, expect } from 'vitest';
// 테스트는 실제 앱 동작 확인 후 개선 예정
// 현재는 실제 기능이 작동하는지 확인하는 것이 우선

describe('useRoutineScheduler', () => {
  it('placeholder test', () => {
    expect(true).toBe(true);
  });
});
