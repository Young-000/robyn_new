# API 설정 가이드

실제 날씨 및 교통 정보를 받기 위해 API 키를 설정해야 합니다.

## 1. 날씨 API (OpenWeatherMap)

### API 키 발급
1. https://openweathermap.org/api 접속
2. 회원가입 후 API 키 발급
3. 무료 플랜으로도 충분합니다 (1분당 60회 호출)

### 설정 방법
1. `frontend/.env` 파일 생성 (또는 `.env.local`)
2. 다음 내용 추가:
```
VITE_WEATHER_API_KEY=your_api_key_here
```

### 사용 가능한 기능
- 현재 날씨 정보 (온도, 날씨 상태, 습도)
- 미세먼지 정보 (PM10, PM2.5, AQI)

## 2. 교통 API (공공데이터포털)

### API 키 발급
1. https://www.data.go.kr/ 접속
2. 회원가입
3. "서울시 버스 도착정보" 또는 "서울시 지하철 도착정보" 검색
4. 활용신청 후 API 키 발급

### 설정 방법
1. `frontend/.env` 파일에 추가:
```
VITE_TRANSIT_API_KEY=your_api_key_here
```

### 사용 가능한 기능
- 버스 도착 정보
- 지하철 도착 정보

## 3. API 키 없이 사용하기

API 키가 없어도 Mock 데이터로 동작합니다:
- 날씨: 랜덤 데이터 (10-30도, 맑음/흐림/비)
- 미세먼지: 랜덤 데이터
- 버스/지하철: 랜덤 도착 시간

하지만 실제 정보를 받으려면 API 키가 필요합니다.

## 4. 환경 변수 설정 예시

`frontend/.env` 파일:
```env
# Weather API (OpenWeatherMap)
VITE_WEATHER_API_KEY=abc123def456ghi789

# Transit API (공공데이터포털)
VITE_TRANSIT_API_KEY=xyz789uvw456rst123
```

## 5. 위치 설정 (날씨/미세먼지)

루틴 추가 시 위치를 설정할 수 있습니다:
- 위도/경도 입력
- 또는 주소 입력 (선택사항)

예시:
- 서울시청: 위도 37.5665, 경도 126.9780
- 강남역: 위도 37.4980, 경도 127.0276

## 6. 버스/지하철 설정

### 버스
- 정류장 ID: 공공데이터포털에서 확인
- 노선 ID: 공공데이터포털에서 확인
- 정류장 이름: 예) "강남역"
- 노선 이름: 예) "146번"

### 지하철
- 역 ID: 공공데이터포털에서 확인
- 호선 ID: 예) "2" (2호선)
- 방향: 상행/하행 선택
- 역 이름: 예) "강남역"
- 호선 이름: 예) "2호선"

## 7. 테스트

API 키를 설정한 후:
1. 개발 서버 재시작: `npm run dev`
2. 루틴 추가 시 실제 정보 소스 선택
3. 알림 시간 설정 후 테스트

## 참고

- API 키는 `.env` 파일에 저장되며 Git에 커밋되지 않습니다 (`.gitignore`에 포함)
- 환경 변수는 `VITE_` 접두사가 필요합니다 (Vite 특성)
- API 호출 실패 시 자동으로 Mock 데이터로 폴백됩니다
